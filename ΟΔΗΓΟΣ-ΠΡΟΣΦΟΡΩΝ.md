# Πώς προσθέτω νέα προσφορά

Οι κάρτες προσφορών **δεν** είναι γραμμένες στο `index.html`. Παράγονται από το
`assets/data/offers.json` από τον renderer `assets/js/offer-renderer.js`.
Για να προσθέσεις προσφορά αρκεί να επεξεργαστείς **ένα αρχείο**: το JSON.

---

## Τα βήματα

1. Άνοιξε το `assets/data/offers.json` και πρόσθεσε ένα αντικείμενο στον πίνακα `offers`.
2. Τρέξε `npm run validate:offers` — ελέγχει ότι δεν λείπει τίποτα.
3. Τρέξε `npm run build && npm run check`.
4. Commit και deploy.

Δεν χρειάζεται να αγγίξεις HTML, CSS ή JavaScript.

---

## Ελάχιστο πρότυπο (αντίγραψέ το)

```json
{
  "id": "cosmote-kiniti",
  "active": true,
  "sortOrder": 60,
  "provider": "Cosmote",
  "title": "Cosmote Κινητή 50GB",
  "cardOfferName": "Cosmote Κινητή",
  "category": "mobile",
  "badge": "50GB κάθε μήνα",
  "shortDescription": "Μηνιαίο πρόγραμμα κινητής για μέλη.",
  "price": "12,90€",
  "period": "μήνα",
  "pricing": {
    "prefix": "",
    "amount": "12,90€",
    "unit": "/ μήνα",
    "note": "Τιμή με ΦΠΑ"
  },
  "benefits": [
    "50 GB δεδομένα κάθε μήνα",
    "Απεριόριστη ομιλία προς όλους",
    "Χωρίς δέσμευση"
  ],
  "ctaPrimaryText": "Με ενδιαφέρει",
  "ctaSecondaryText": "Περισσότερα",
  "ctaType": "modal",
  "modalId": "internetChoiceModal",
  "actionTarget": {
    "track": "offer_interest_click",
    "offer": "Cosmote Κινητή",
    "category": "mobile"
  }
}
```

---

## Τι σημαίνει κάθε πεδίο

### Υποχρεωτικά

| Πεδίο | Τι είναι | Κανόνας |
|---|---|---|
| `id` | Μοναδικό αναγνωριστικό | Μόνο πεζά και παύλες, π.χ. `cosmote-kiniti`. Δεν επιτρέπεται διπλό. |
| `provider` | Όνομα παρόχου | Δίνει και το χρώμα της κορδέλας (δες παρακάτω). |
| `title` | Τίτλος κάρτας | Εμφανίζεται κεντραρισμένος. |
| `category` | Κατηγορία | Μόνο: `mobile`, `internet`, `tv`, `guide`, `other`. Ορίζει σε ποιο φίλτρο εμφανίζεται. |
| `price` | Τιμή ως κείμενο | Κρατιέται για συμβατότητα. Το τι βλέπει ο χρήστης το ορίζει το `pricing`. |
| `benefits` | Πίνακας κειμένων | **Εμφανίζονται μόνο τα 3 πρώτα** στην κάρτα. Τα υπόλοιπα φαίνονται στις Λεπτομέρειες. |
| `ctaType` | Τι κάνει το κουμπί | Μόνο `modal` ή `activation-guide`. |
| `actionTarget` | Στοιχεία tracking | Θέλει οπωσδήποτε `track`, `offer`, `category`. |

### Η τιμή — `pricing`

Αυτό είναι το σημαντικότερο για να συγκρίνονται σωστά οι προσφορές.
**Βάζε πάντα μηνιαία τιμή**, ακόμη κι αν η προσφορά πληρώνεται μπροστά:

```json
"pricing": {
  "prefix": "από",
  "amount": "8,3€",
  "unit": "/ μήνα",
  "note": "100€ προπληρωμή για 12 μήνες"
}
```

- `prefix` — μικρό κείμενο πριν τον αριθμό, π.χ. `από`. Άφησέ το `""` αν δεν χρειάζεται.
- `amount` — ο μεγάλος αριθμός.
- `unit` — π.χ. `/ μήνα`.
- `note` — η γραμμή με τα ψιλά γράμματα από κάτω.

Αν παραλείψεις το `pricing`, ο renderer προσπαθεί να μαντέψει από το `price`
και το `period`. Δουλεύει, αλλά το αποτέλεσμα είναι λιγότερο ελεγχόμενο.

### Προαιρετικά

- `active` — βάλε `false` για να **κρύψεις** προσφορά χωρίς να τη σβήσεις.
- `sortOrder` — αριθμός· μικρότερος αριθμός εμφανίζεται πρώτος.
- `badge` — μικρό pill δεξιά στην κορδέλα. Κράτα το σύντομο, κόβεται με «…».
- `shortDescription` — μία γραμμή κάτω από τον τίτλο.
- `details`, `includes`, `plans`, `documents`, `notes` — εμφανίζονται στο παράθυρο «Λεπτομέρειες».

---

## Τα δύο είδη κουμπιού

### `ctaType: "modal"` — ανοίγει παράθυρο

Θέλει `modalId` που **πρέπει** να υπάρχει στο μητρώο του `assets/js/modals.js`.
Διαθέσιμα σήμερα:

`activationProviderChoiceModal`, `activationGuideModal`, `novaLinePhone`,
`novaEonModal`, `internetChoiceModal`, `vodafoneFixedModal`, `infoCoopModal`

### `ctaType: "activation-guide"` — ανοίγει τον οδηγό ενεργοποίησης

Θέλει μέσα στο `actionTarget`:

```json
"actionTarget": {
  "activationProvider": "vodafone",
  "activationOffer": "Vodafone CU",
  "activationSource": "offer_interest",
  "track": "offer_interest_click",
  "offer": "Vodafone CU",
  "category": "mobile"
}
```

Το κείμενο «Οδηγός ενεργοποίησης» μετατρέπεται αυτόματα σε **«Ενεργοποίηση»**
και το «Περισσότερα» σε **«Λεπτομέρειες»** στην κάρτα.

---

## Χρώμα κορδέλας ανά πάροχο

Ορίζεται στο `PROVIDER_COLORS` μέσα στο `assets/js/offer-renderer.js`:

| Πάροχος | Χρώμα |
|---|---|
| Vodafone | `#e60000` |
| Nova / Q | `#0a2896` |
| Cosmote | `#7ac143` |
| Wind | `#0067b1` |
| EON | `#5c2d91` |

Για νέο πάροχο πρόσθεσε μια γραμμή εκεί. Αν δεν υπάρχει, χρησιμοποιείται
σκούρο μπλε (`#16243d`).

---

## Συχνά λάθη

- **Αρχεία PDF**: κάθε `href` προς τοπικό αρχείο ελέγχεται. Αν το PDF δεν
  υπάρχει στο `assets/docs/`, το `validate:offers` κόβει το build.
- **Άγνωστο `modalId`**: αν δεν είναι στη λίστα του `modals.js`, αποτυγχάνει ο έλεγχος.
- **Διπλό `id`**: αποτυγχάνει ο έλεγχος.
- **Πάνω από 3 benefits**: δεν είναι λάθος, αλλά μόνο τα 3 πρώτα φαίνονται στην κάρτα.
- **Ξεχασμένο `pricing`**: η κάρτα θα δείξει το ακατέργαστο `price`, που μπορεί
  να μη συγκρίνεται με τις υπόλοιπες προσφορές.
