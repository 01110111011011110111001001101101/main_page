# Τεχνικός έλεγχος — τι χωρίζει τον κώδικα από επίπεδο senior

Έλεγχος ολόκληρου του repo. Τα ευρήματα είναι ταξινομημένα κατά **λόγο κόστους/οφέλους**,
όχι κατά σοβαρότητα. Κάθε σημείο έχει μετρήσιμο στοιχείο, όχι γνώμη.

Τι είναι ήδη καλό και **δεν** πρέπει να χαλάσει: τα build scripts με content hashing,
τα `check-*.mjs` ως quality gates, το `validate-offers.mjs`, τα ελληνικά σχόλια που
εξηγούν το «γιατί», το lazy loading των modals, τα passive scroll listeners.
Αυτά είναι πάνω από τον μέσο όρο.

---

## 1. Το CSS είναι το πραγματικό χρέος

**Τα νούμερα:** 12.278 γραμμές, **1.345 `!important`**, 102 media queries,
`.site-top-nav-inner` δηλωμένο 7 φορές, `.modal-backdrop` 6, `.offer-grid` 4.
Το bundle είναι 257 KB ασυμπίεστο (44 KB gzip — μέσα στο όριο, αλλά το parse
cost στο κινητό είναι πραγματικό).

**Γιατί μετράει:** το `!important` δεν είναι στιλιστική επιλογή, είναι σύμπτωμα.
Κάθε φορά που κάποιος δεν μπορούσε να νικήσει έναν κανόνα, πρόσθετε ένα ακόμα.
Το είδαμε τρεις φορές στη σειρά σε αυτή τη συνεδρία: η μπάρα που δεν κρυβόταν,
οι κάρτες που έσπαγαν στο κινητό, το κείμενο που έφευγε αριστερά. Και τα τρία
ήταν μάχες specificity, όχι λάθη λογικής. Ο χρόνος που χάνεται εκεί είναι ο
μεγαλύτερος κρυφός φόρος του project.

**Πρόταση, με σειρά προτεραιότητας:**

1. **Σβήσε τον νεκρό κώδικα πρώτα.** Επιβεβαιωμένα αχρησιμοποίητοι selectors —
   δεν υπάρχουν πουθενά σε HTML ή JS:

   | Selector | Εμφανίσεις στο CSS |
   |---|---|
   | `.offer-card--glass` | 83 |
   | `.offer-card-mobile` | 25 |
   | `.offer-card__price-panel` | 16 |
   | `.offer-card-top` | 13 |
   | `.offer-highlight-badge` | 12 |
   | `.offer-note` | 11 |
   | `.offer-category` | 10 |
   | `.offer-card__spec-pill` / `__title-row` / `__type` | 9 |
   | `.top-brand-text` | 2 |

   Είναι υπολείμματα από τις παλιές κάρτες. Εκτίμηση: **1.500–2.000 γραμμές**
   φεύγουν χωρίς καμία οπτική αλλαγή. Ασφαλής τρόπος: σβήσε, χτίσε, τρέξε το
   `tools/smoke-test.mjs` και σύγκρινε screenshots.

2. **Ένα media query breakpoint, όχι τέσσερα.** Τώρα συνυπάρχουν
   `max-width: 767px`, `max-width: 768px`, `max-width: 640px` και
   `min-width: 768px`. Το `767` και το `768` μαζί δημιουργούν κενό ενός pixel
   όπου δεν ισχύει κανένας κανόνας. Όρισε δύο breakpoints ως custom media
   και χρησιμοποίησέ τα παντού.

3. **Καθάρισε το `offers.json` από styling.** Τα πεδία `style.cardClasses`,
   `style.priceClasses` και `brandSkin` (15 εμφανίσεις) δεν διαβάζονται πια από
   τον renderer — μόνο το `brandSkin` γράφεται σε `dataset` χωρίς αναγνώστη.
   Δεδομένα και παρουσίαση δεν πρέπει να μπλέκονται· αυτή είναι η αιτία που οι
   κάρτες ήταν τόσο δύσκολο να ξαναγραφτούν.

---

## 2. Δύο διαφορετικά μοντέλα module στο ίδιο codebase

**Το εύρημα:** 5 αρχεία είναι IIFE (`main`, `offer-renderer`, `office-closure`,
`pdf-preview`, `wizard`), 8 είναι top-level scripts που βασίζονται στο ότι το
esbuild τα ενώνει σε κοινό scope (`clipboard`, `config`, `image-preview`,
`modals`, `offers`, `office-closure.config`, `tracking`, `ui`).
Υπάρχουν **43 αναθέσεις σε `window.*`** ως τρόπος επικοινωνίας.

**Γιατί μετράει:** το ότι το `offers.js` βλέπει συναρτήσεις του `tracking.js`
είναι σύμπτωση του bundler, όχι δηλωμένη εξάρτηση. Δεν φαίνεται ποιος χρειάζεται
ποιον, το tree-shaking είναι αδύνατο, και η σειρά στο `build-js-bundle.mjs`
γίνεται κρυφό συμβόλαιο. Το είδα στην πράξη: για να τεστάρω το `ui.js` έπρεπε
να ενώσω έξι αρχεία σε ένα `eval`.

**Πρόταση:** πέρασε σε ES modules με ρητά `import`/`export`. Το esbuild το
υποστηρίζει ήδη και το κάνει bundle το ίδιο. Κράτα το `window.App` **μόνο** ως
δημόσιο API προς τα modals και τα inline handlers, με σχόλιο ότι αυτό είναι το
σύνορο. Η μετάβαση γίνεται αρχείο-αρχείο χωρίς μεγάλο refactor.

---

## 3. Τέσσερις ξεχωριστοί scroll listeners

**Πού:** `main.js:83`, `tracking.js:665`, `ui.js:131`, `ui.js:348`.
Καθένας διαβάζει `window.scrollY` ή καλεί `getBoundingClientRect()`.

**Γιατί μετράει:** κάθε `getBoundingClientRect()` σε scroll handler προκαλεί
forced synchronous layout. Τέσσερις ανεξάρτητοι handlers σημαίνει μέχρι
τέσσερα reflows ανά frame στο κινητό. Επίσης το `main.js` και το `ui.js`
χειρίζονται **και τα δύο** την εμφάνιση της πάνω μπάρας (`is-scrolled` και
`site-top-nav-hidden`) — δύο συστήματα για το ίδιο στοιχείο, που είναι ακριβώς
ο λόγος που η μπάρα ήταν τόσο μπερδεμένη.

**Πρόταση:** ένας κεντρικός `scroll-coordinator` που τρέχει μία φορά ανά
`requestAnimationFrame`, διαβάζει τα μετρικά μία φορά και ειδοποιεί συνδρομητές.
Όπου γίνεται, αντικατάστησε με `IntersectionObserver` — το `tracking.js` το
χρησιμοποιεί ήδη σωστά για τις κάρτες, οπότε το μοτίβο υπάρχει.

---

## 4. Μηδέν unit tests

**Το εύρημα:** υπάρχουν `smoke-test.mjs` και `dead-code-audit.mjs` (και τα δύο
απαιτούν Playwright), αλλά **κανένα** αρχείο `*.test.*`. Τα `npm run check`
ελέγχουν σύνταξη και μεγέθη, όχι συμπεριφορά.

**Γιατί μετράει:** σε αυτή τη συνεδρία έγραψα ad-hoc jsdom scripts για να
επαληθεύσω τον renderer, τη λογική scroll και τη CSS specificity. Βρήκαν
πραγματικά bugs — το whole-card click που άνοιγε modal όταν διάβαζες τις
παροχές, και τον τίτλο που έχανε τη μάχη specificity. Αυτά τα tests θα έπρεπε
να ζουν στο repo, όχι σε προσωρινό φάκελο.

**Πρόταση:** πρόσθεσε `node --test` με jsdom (ήδη dependency) και ξεκίνα από τα
τρία σημεία με το μεγαλύτερο ρίσκο: `normalizeOffers` + `renderOfferCard`
(σωστά attributes και τιμές), `splitPrice`/`getPricing` (parsing τιμών),
`mapCtaText` (αντικαταστάσεις κειμένου). Είναι καθαρές συναρτήσεις — εύκολα
tests, μεγάλη κάλυψη ρίσκου.

---

## 5. Ο renderer κάνει τρεις δουλειές

**Το εύρημα:** το `offer-renderer.js` (703 γραμμές) περιέχει fetch, DOM
building, **και** ολόκληρο το details modal με routing μέσω `history.pushState`.

**Πρόταση:** χώρισέ το σε `offers-data.js` (fetch, normalize, cache),
`offer-card.js` (καθαρή `offer → HTMLElement`) και `offer-details-modal.js`
(modal + routing). Το `offer-card.js` γίνεται τότε τετριμμένα testable.

---

## 6. Μικρότερα, γρήγορα

- **Κενά `catch`** στο `ui.js:379` και `ui.js:400` (`catch (_e) {}`) — καταπίνουν
  σφάλματα σιωπηλά. Βάλε τουλάχιστον σχόλιο γιατί αγνοείται.
- **Timers χωρίς cleanup:** 29 `setTimeout`/`setInterval` σε 8 αρχεία, κανένα
  `clearTimeout` σε teardown. Σε SPA θα ήταν διαρροή· εδώ είναι ανεκτό, αλλά ο
  typewriter που πρόσθεσα π.χ. συνεχίζει αν φύγει το στοιχείο.
- **Το `cardModalTarget`** (`offers.js:95`) γράφεται και δεν διαβάζεται πουθενά.
- **Το critical CSS είναι χειροκίνητο** (`assets/css/critical.css`) και
  αντιγράφει κανόνες από το `site.css`. Ήδη είχαν αποκλίνει: ο τίτλος του hero
  ήταν κρυμμένος εκεί ενώ στο `site.css` ήταν ορατός. Είτε παρήγαγέ το
  αυτόματα, είτε βάλε σχόλιο-προειδοποίηση στην κορυφή και των δύο αρχείων.
- **Τρία αρχεία τεκμηρίωσης** (`AUDIT_2026-07-20.md`, `CODE-REVIEW.md`,
  `PERFORMANCE-UI.md`) με επικαλυπτόμενο περιεχόμενο και ημερομηνίες στα ονόματα.
  Ένωσέ τα σε ένα `DEVELOPMENT.md`· το git κρατάει το ιστορικό.

---

## Προτεινόμενη σειρά

| # | Ενέργεια | Κόστος | Όφελος |
|---|---|---|---|
| 1 | Διαγραφή νεκρού CSS των παλιών καρτών | μισή μέρα | ~2.000 γραμμές, άμεσα λιγότερες συγκρούσεις |
| 2 | Unit tests στις καθαρές συναρτήσεις του renderer | μισή μέρα | σταματά τα regressions |
| 3 | Ενοποίηση scroll handlers | 1 μέρα | performance + τέλος στη διπλή λογική της μπάρας |
| 4 | Καθαρισμός styling από το `offers.json` | 2 ώρες | δεδομένα χωρίς παρουσίαση |
| 5 | Μετάβαση σε ES modules | 1–2 μέρες | ρητές εξαρτήσεις, tree-shaking |
| 6 | Σπάσιμο του `offer-renderer.js` | 1 μέρα | testability |

Τα 1, 2 και 4 δίνουν το μεγαλύτερο όφελος ανά ώρα. Θα ξεκινούσα από εκεί.
